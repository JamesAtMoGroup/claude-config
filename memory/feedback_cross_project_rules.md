---
name: Cross-project universal rules (Vibe Coding + article-video)
description: Both Remotion projects share three universal rules despite having separate skill files
type: feedback
---

Both Vibe Coding 剪輯 and article-video are separate projects with their own SKILL.md / course-video.md files, but these three rules apply to both without exception:

1. **Visual style** — Always use the Vibe Coding dark neon-green aesthetic (`#000000` bg, `#7cffb2` primary, `#ffd166` yellow, Noto Sans TC + Space Mono, no emoji in main content cards)
2. **4K output** — All renders must be 3840×2160; article-video uses `S = 3` (1280×720 base), Vibe Coding uses `S = 2` (1920×1080 base)
3. **iMessage notification motion graphics** — All motion graphic callouts must use the macOS dark frosted-glass iMessage banner style with stacking push-down behavior (no old left/right floating cards)

**Why:** User confirmed: "my rules stays the same. Same styling, same video results quality, same notification-style motion graphics"

**How to apply:** When working on either Remotion project, apply all three rules regardless of which project-specific skill file is being used. The skill files may differ in workflow details but the visual/output/motion standards are identical.
